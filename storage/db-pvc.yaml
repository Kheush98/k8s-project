apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  labels:
    app: gti
spec:
  storageClassName: kops-csi-1-21
  resources:
    requests:
      storage: 3Gi
  accessModes:
    - ReadWriteOnce
